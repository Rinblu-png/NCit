# 🎓 ระบบจัดการเว็บไซต์วิทยาลัยอาชีวศึกษานครปฐม

## 📋 รายละเอียดระบบ

ระบบเว็บไซต์สำหรับวิทยาลัยอาชีวศึกษา พร้อมระบบจัดการข้อมูล (Admin Panel) ที่ใช้ PHP + MySQL

### ✨ ฟีเจอร์หลัก

#### 🌐 หน้าเว็บไซต์หลัก (index.php)
- **หน้าหลัก** - แสดงภาพรวมและสถิติสำคัญ
- **หน้าผู้บริหาร** - ข้อมูลคณะผู้บริหาร
- **หน้าบุคลากร** - ข้อมูลคณาจารย์และเจ้าหน้าที่
- **หน้านักเรียน** - สถิติและกิจกรรมนักเรียน นักศึกษา
- **หน้าสาขาวิชา** - รายละเอียดสาขาวิชาที่เปิดสอน
- Responsive Design (รองรับทุกอุปกรณ์)
- ดีไซน์มินิมอล สีสวย ใช้งานง่าย

#### 🔧 ระบบจัดการ (admin.php)
- เพิ่ม/แก้ไข/ลบ ข้อมูลผู้บริหาร
- เพิ่ม/แก้ไข/ลบ ข้อมูลบุคลากร
- อัพเดทข้อมูลสถิตินักเรียน
- เพิ่ม/แก้ไข/ลบ ข้อมูลสาขาวิชา
- UI ทันสมัย ใช้งานง่าย

## 📁 โครงสร้างไฟล์

```
project/
│
├── index.php                 # หน้าเว็บไซต์หลัก
├── admin.php                 # หน้าจัดการข้อมูล (Admin)
├── config.php                # ไฟล์ตั้งค่าฐานข้อมูล
│
├── api_executives.php        # API ดึงข้อมูลผู้บริหาร
├── api_staff.php             # API ดึงข้อมูลบุคลากร
├── api_students.php          # API ดึงข้อมูลนักเรียน
├── api_departments.php       # API ดึงข้อมูลสาขาวิชา
│
├── save_data.php             # บันทึกข้อมูล (Create/Update)
├── delete_data.php           # ลบข้อมูล
├── update_students.php       # อัพเดทข้อมูลนักเรียน
│
├── style.css                 # CSS หลัก
├── script.js                 # JavaScript หน้าเว็บไซต์
├── admin_script.js           # JavaScript หน้า Admin
│
├── database.sql              # ไฟล์ SQL สำหรับสร้างฐานข้อมูล
└── README.txt                # คำแนะนำการติดตั้ง
```

## 🚀 การติดตั้ง

### ข้อกำหนดระบบ
- PHP 7.4 ขึ้นไป
- MySQL 5.7 ขึ้นไป หรือ MariaDB
- Web Server (Apache, Nginx หรือใช้ XAMPP/WAMP)
- Web Browser ที่ทันสมัย (Chrome, Firefox, Safari, Edge)

### ขั้นตอนการติดตั้ง

#### 1. ติดตั้ง XAMPP (สำหรับ Windows)
- ดาวน์โหลด XAMPP จาก https://www.apachefriends.org/
- ติดตั้งและเปิดใช้งาน Apache และ MySQL

#### 2. วางไฟล์โปรเจค
- คัดลอกไฟล์ทั้งหมดไปที่ `C:\xampp\htdocs\college\`
- หรือ path อื่นตามที่ต้องการ

#### 3. สร้างฐานข้อมูล

**วิธีที่ 1: ใช้ phpMyAdmin**
1. เปิด http://localhost/phpmyadmin
2. คลิก "New" เพื่อสร้างฐานข้อมูลใหม่
3. ตั้งชื่อ: `nakhonpathom_college`
4. Collation: `utf8mb4_unicode_ci`
5. คลิก "Import"
6. เลือกไฟล์ `database.sql`
7. คลิก "Go"

**วิธีที่ 2: ใช้ Command Line**
```bash
mysql -u root -p
CREATE DATABASE nakhonpathom_college CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE nakhonpathom_college;
SOURCE database.sql;
```

#### 4. ตั้งค่าการเชื่อมต่อฐานข้อมูล
แก้ไขไฟล์ `config.php` (ถ้าจำเป็น):
```php
define('DB_HOST', 'localhost');      
define('DB_USER', 'root');           
define('DB_PASS', '');               // ใส่รหัสผ่าน MySQL ถ้ามี
define('DB_NAME', 'nakhonpathom_college');
```

#### 5. เข้าใช้งานระบบ
- เว็บไซต์หลัก: http://localhost/Ncme/index.php
- ระบบจัดการ: http://localhost/Ncme/admin.php

## 📖 คู่มือการใช้งาน

### 🌐 การใช้งานเว็บไซต์หน้าบ้าน

1. เปิด `http://localhost/Ncme/index.php`
2. คลิกเมนูด้านบนเพื่อดูข้อมูลแต่ละหน้า
3. ข้อมูลจะถูกดึงจากฐานข้อมูลอัตโนมัติ

### 🔧 การใช้งานระบบจัดการ (Admin)

#### เข้าสู่ระบบ Admin
1. เปิด `http://localhost/Ncme/admin.php`
2. เลือกแท็บที่ต้องการจัดการ

#### การจัดการข้อมูลผู้บริหาร
1. คลิกแท็บ "ผู้บริหาร"
2. คลิก "+ เพิ่มผู้บริหาร" เพื่อเพิ่มข้อมูล
3. กรอกข้อมูล:
   - ชื่อ-นามสกุล (*)
   - ตำแหน่ง (*)
   - การศึกษา
   - ประสบการณ์
   - รายละเอียด
   - ไอคอน (Emoji)
4. คลิก "บันทึก"

#### การแก้ไขข้อมูล
1. คลิกปุ่ม "แก้ไข" ที่ข้อมูลที่ต้องการ
2. แก้ไขข้อมูลในฟอร์ม
3. คลิก "บันทึก"

#### การลบข้อมูล
1. คลิกปุ่ม "ลบ" ที่ข้อมูลที่ต้องการ
2. ยืนยันการลบ

#### การจัดการข้อมูลบุคลากร
- ขั้นตอนเหมือนกับการจัดการผู้บริหาร
- เพิ่มฟิลด์ "แผนก" และ "ความเชี่ยวชาญ"

#### การอัพเดทข้อมูลนักเรียน
1. คลิกแท็บ "นักเรียน"
2. แก้ไขตัวเลขสถิติ:
   - จำนวนนักเรียน นักศึกษาทั้งหมด
   - จำนวนระดับ ปวช.
   - จำนวนระดับ ปวส.
   - อัตราการมีงานทำ (%)
3. คลิก "บันทึกข้อมูล"

#### การจัดการสาขาวิชา
1. คลิกแท็บ "สาขาวิชา"
2. คลิก "+ เพิ่มสาขาวิชา"
3. กรอกข้อมูล:
   - ชื่อสาขาวิชา (*)
   - ประเภท (*) - เลือกจาก dropdown
   - ระดับที่เปิดสอน
   - ความเชี่ยวชาญ
   - โอกาสทางอาชีพ
   - ไอคอน (Emoji)
4. คลิก "บันทึก"

## 🗄️ โครงสร้างฐานข้อมูล

### ตาราง `executives` (ผู้บริหาร)
- id (INT, Primary Key, Auto Increment)
- name (VARCHAR 255) - ชื่อ-นามสกุล
- position (VARCHAR 255) - ตำแหน่ง
- education (VARCHAR 255) - การศึกษา
- experience (VARCHAR 100) - ประสบการณ์
- description (TEXT) - รายละเอียด
- avatar_icon (VARCHAR 50) - ไอคอน
- created_at, updated_at (TIMESTAMP)

### ตาราง `staff` (บุคลากร)
- id (INT, Primary Key, Auto Increment)
- name (VARCHAR 255) - ชื่อ-นามสกุล
- position (VARCHAR 255) - ตำแหน่ง
- department (VARCHAR 255) - แผนก
- education (VARCHAR 255) - การศึกษา
- expertise (TEXT) - ความเชี่ยวชาญ
- avatar_icon (VARCHAR 50) - ไอคอน
- created_at, updated_at (TIMESTAMP)

### ตาราง `students` (นักเรียน - สถิติ)
- id (INT, Primary Key, Auto Increment)
- total_students (INT) - จำนวนนักเรียนทั้งหมด
- vocational_students (INT) - นักเรียน ปวช.
- diploma_students (INT) - นักเรียน ปวส.
- employment_rate (DECIMAL) - อัตราการมีงานทำ (%)
- updated_at (TIMESTAMP)

### ตาราง `departments` (สาขาวิชา)
- id (INT, Primary Key, Auto Increment)
- name (VARCHAR 255) - ชื่อสาขาวิชา
- category (VARCHAR 100) - ประเภท
- icon (VARCHAR 50) - ไอคอน
- levels (VARCHAR 255) - ระดับที่เปิดสอน
- expertise (TEXT) - ความเชี่ยวชาญ
- career_opportunities (TEXT) - โอกาสทางอาชีพ
- created_at, updated_at (TIMESTAMP)

### ตาราง `student_activities` (กิจกรรมนักเรียน)
- id (INT, Primary Key, Auto Increment)
- title (VARCHAR 255) - ชื่อกิจกรรม
- description (TEXT) - รายละเอียด
- icon (VARCHAR 50) - ไอคอน
- created_at (TIMESTAMP)

## 🔒 ความปลอดภัย

### การป้องกัน SQL Injection
- ใช้ฟังก์ชัน `sanitize()` ในการทำความสะอาดข้อมูล
- ใช้ `real_escape_string()` กับทุก input

### คำแนะนำเพิ่มเติม
1. เปลี่ยนรหัสผ่านฐานข้อมูล
2. ควรเพิ่มระบบ Login สำหรับ Admin (ตอนนี้ยังไม่มี)
3. ตั้งค่า HTTPS สำหรับการใช้งานจริง
4. สำรองข้อมูลเป็นประจำ

## ⚙️ การแก้ไขปัญหาที่พบบ่อย

### ปัญหา: ไม่สามารถเชื่อมต่อฐานข้อมูล
**วิธีแก้:**
1. ตรวจสอบ MySQL ทำงานอยู่หรือไม่ (ใน XAMPP Control Panel)
2. ตรวจสอบ username/password ใน `config.php`
3. ตรวจสอบชื่อฐานข้อมูลถูกต้อง

### ปัญหา: แสดงข้อมูลภาษาไทยเป็น ????
**วิธีแก้:**
1. ตรวจสอบ charset ในฐานข้อมูลเป็น `utf8mb4_unicode_ci`
2. ตรวจสอบ `$conn->set_charset("utf8mb4");` ใน config.php

### ปัญหา: หน้าเว็บขึ้น 404 Not Found
**วิธีแก้:**
1. ตรวจสอบ path ของไฟล์ถูกต้อง
2. ตรวจสอบ Apache ทำงานอยู่
3. ลองเข้า http://localhost เพื่อทดสอบ

### ปัญหา: ข้อมูลไม่อัพเดท
**วิธีแก้:**
1. กด Ctrl + F5 (Hard Refresh)
2. ล้าง Browser Cache
3. ตรวจสอบ Console ใน Browser (F12)

## 📝 การปรับแต่งเพิ่มเติม

### เปลี่ยนสีธีม
แก้ไขไฟล์ `style.css`:
```css
/* หาบรรทัดที่มี gradient */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

/* เปลี่ยนเป็นสีที่ต้องการ เช่น */
background: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 100%);
```

### เพิ่มข้อมูลติดต่อ
แก้ไขส่วน Footer ในไฟล์ `index.php`

### เพิ่ม Logo
1. เตรียมไฟล์ logo (logo.png)
2. วางไฟล์ในโฟลเดอร์โปรเจค
3. แก้ไข Navigation ในไฟล์ `index.php`:
```html
<a href="#" class="logo">
    <img src="logo.png" alt="Logo" style="height: 40px;">
    วิทยาลัยอาชีวศึกษานครปฐม
</a>
```

## 📞 ติดต่อและสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Console ใน Browser (กด F12)
2. ตรวจสอบ Error Log ของ PHP
3. อ่านคู่มือนี้อีกครั้ง

## 📄 License

โปรเจคนี้สามารถนำไปใช้และแก้ไขได้ฟรี สำหรับวัตถุประสงค์ทางการศึกษา

---

**เวอร์ชัน:** 1.0
**วันที่:** 2025
**พัฒนาโดย:** Claude AI

🎉 ขอให้โชคดีกับการพัฒนาเว็บไซต์!
